# AWS Amplify Redirect Rules
# These rules prevent 301 redirects and handle routing properly

# Remove trailing slashes from API routes first
/api/*/  /api/:splat  301

# API routes - pass through (will be handled by Lambda function if configured)
/api/*  /api/:splat  200

# Health check endpoint
/api/health  /api/health  200

# SPA fallback - serve index.html for all other routes
# This prevents 301 redirects for client-side routing
/*  /index.html  200

